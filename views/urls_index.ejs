<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>

  <body>
    <header>
      <% include ../views/partials/_header %>
    </header>

   <ul>
      <% for(var i in urls) { %>
    <li>Shortened URLs: <%= i %> - <%= urls[i] %></li>
      <% } %>
      <li>Add New URL</li>
   </ul>

  </body>
</html>
